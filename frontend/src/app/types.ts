//Note: create global export for interface?? to prevent repetition of code
export interface User {
  email: string;
  password: string;
  _id: string;
  activityTime: number;
  firstName: string;
  lastName: string;
}
